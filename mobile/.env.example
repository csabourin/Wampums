# Wampums React Native App - Environment Configuration Template
# Copy this file to .env and update with your local values

# API Configuration
# For local development, use your computer's IP address (not localhost)
# Example: http://192.168.1.100:3000/api
API_URL=http://localhost:3000/api
API_VERSION=v1

# Environment (development, staging, production)
NODE_ENV=development

# Feature Flags
ENABLE_DEBUG_LOGGING=true
ENABLE_OFFLINE_MODE=true
ENABLE_PUSH_NOTIFICATIONS=true

# Organization (for development)
# If you want to default to a specific organization during development
# DEFAULT_ORG_ID=

# Sentry (optional - for error tracking)
# SENTRY_DSN=

# Notes for local development:
# 1. For Android emulator to reach host machine: use 10.0.2.2:3000
#    API_URL=http://10.0.2.2:3000/api
# 2. For iOS simulator to reach host machine: use localhost or 127.0.0.1:3000
#    API_URL=http://localhost:3000/api
# 3. For physical devices: use your computer's IP address on the same network
#    API_URL=http://192.168.1.XXX:3000/api
